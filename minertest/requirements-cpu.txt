# Minimal dependencies for CPU mining on Akash
# NO PyTorch - SA miner uses dwave-ocean-sdk's built-in SimulatedAnnealingSampler
# Excludes visualization (matplotlib, pandas, seaborn) and GPU packages (torch, cupy)

# Core D-Wave SDK for Ising model and sampling
dwave-ocean-sdk>=6.0.0

# Numerical computing
numpy>=1.24.0

# Async HTTP for P2P networking
aiohttp>=3.9.0

# CLI interface
click>=8.1.7

# Environment variable loading
python-dotenv>=1.0.0

# TOML config parsing (included in Python 3.11+, but listed for compatibility)
tomli>=2.0.1; python_version<'3.11'

# Post-quantum signatures (SPHINCS+)
hashsigs>=0.0.2

# Fast hashing
blake3>=1.0.5